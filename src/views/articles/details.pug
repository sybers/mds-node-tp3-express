extends ../layout.pug

block content
    div(class="p-4")
        a(class="px-2 p-1 mb-4 border-2 border-blue-500 rounded-md hover:bg-blue-500 hover:text-white", href=`/articles`) ← Back to articles

        h1(class="text-4xl font-bold")=article.title
        pre=article.createdAt
        p=article.body
